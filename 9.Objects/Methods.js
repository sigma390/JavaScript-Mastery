//====================> Builtin methods Provided by parent Constructor <===========

const OS = {
  name: 'Ubuntu',
  version: 18.4,
  license: 'Open Source',
};

//   Object.keys()
console.log(Object.keys(OS));

//   Object.values()
console.log(Object.values(OS));

// both Key and Values , Object.entries()
console.log(Object.entries(OS));
